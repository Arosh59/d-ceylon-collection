# Server-only ASP.NET Core API origin. Never expose credentials in NEXT_PUBLIC_* values.
API_BASE_URL=http://127.0.0.1:8080

# Canonical public origin used for absolute metadata.
SITE_URL=http://127.0.0.1:3000
